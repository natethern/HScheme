# HScheme
HScheme is a Scheme interpreter written in Haskell.

There are lots of GH repos named hscheme. This is the original HScheme from https://hscheme.sourceforge.net/. Unfortunately, it does not currently compile.
